alert("Quadrado do número 15 a 200");
let contadora = 14;
for (numero = 15; numero <= 200; numero++) {
  contadora = contadora + 1;
  alert(`O quadrado do número ${numero} é ${contadora * contadora}`);
}
